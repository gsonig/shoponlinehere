<%= render :partial => 'spree/shared/error_messages', :locals => { :target => @spree_user } %>
<section id="form">
  <div class="col-md-6 col-md-offset-3" id="forgot-password">
    <div class="login-form">
      <p><%= Spree.t(:instructions_to_reset_password) %></p>
      <h2 class="panel-title"><%= Spree.t(:forgot_password) %></h2>
      <%= form_for Spree::User.new, :as => :spree_user, :url => spree.reset_password_path do |f| %>
        <%= f.email_field :email, :class => "form-control", :placeholder => Spree.t(:email) %>
        <%= f.submit Spree.t(:reset_password), :class => 'btn btn-success btn-checkout' %>
      <% end %>
    </div>
  </div>
</section>